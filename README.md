# Proofline

GitHub Pages-ready Vite + React project.

## Run locally
npm install
npm run dev

## Build
npm run build

The production files are generated in `dist/`.

## GitHub Pages
Set GitHub Pages to deploy using GitHub Actions, or use a Pages workflow that runs `npm ci` and `npm run build`, then deploys `dist/`.
