import React from "react";
import ReactDOM from "react-dom/client";
import Proofline from "./proofline.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Proofline />
  </React.StrictMode>
);
